replicaCount: 2

image:
  repository: quay.io/bkmiles/vault-guestbook-app
  tag: latest
  pullPolicy: Always

# Database config
db:
  # default DB/schema name = release namespace (overridable)
  name: "myapp-db"
  # Option A: inject host directly (string)
  host: "demo-db-2lrghak4kg63df46nn3nt0qc1spbmmv1.chgcwq4a61in.us-east-1.rds.amazonaws.com"
  # Option B: read host from a K8s Secret synced by Vault (preferred)
  hostFromSecret:
    enabled: false
    name: guestbook-kv
    key: host

  # Dynamic DB creds secret (created by VaultDynamicSecret)
  secretName: db-dynamic-creds-guestbook
  usernameKey: username
  passwordKey: password

# Vault declarations (no VaultAuth here; platform pre-provisions it)
vault:
  authRef: vault-auth

  dynamicSecrets:
    - name: db-app-creds
      mount: rosa-2lrghak4kg63df46nn3nt0qc1spbmmv1-database
      path: creds/my-app-role
      allowStaticCreds: false
      refreshAfter: 30s
      renewalPercent: 50
      destination:
        name: db-dynamic-creds-guestbook
        type: Opaque

  staticSecrets:
    - name: guestbook-kv
      mount: openshift-rosa-2lrghak4kg63df46nn3nt0qc1spbmmv1
      type: kv-v2
      path: apps/team-d/my-app/secretdata
      refreshAfter: 10s
      destination:
        name: guestbook-kv
        type: Opaque

  pki:
    - name: guestbook-tls
      mount: pki_int
      role: team-d
      commonName: "guestbook.team-d.example.com"
      altNames:
      ttl: 5m
      refreshAfter: 1m
      format: pem
      destination:
        name: guestbook-tls
        type: kubernetes.io/tls

# DB init job settings
dbInit:
  enabled: true
  image: registry.redhat.io/rhel8/mysql-80
  pullPolicy: IfNotPresent
  # If you must use a Red Hat image, set:
  # image: registry.redhat.io/rhel8/mysql-80
  # (ensure your cluster can pull from registry.redhat.io)

service:
  type: ClusterIP
  port: 5000

route:
  enabled: true
  host: "my-app.apps.rosa.hashifsi.2md7.p3.openshiftapps.com"
  termination: passthrough  # or edge
  tlsSecretName: guestbook-tls

extraEnv: {}
  # MESSAGE: "Hello from Helm"
  # SUPERSECRET: "super-secret-value"
